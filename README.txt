1. Edit CmakeLists.txt to specify allolib's location
2. Build as you want
3. excutables will be in ${BUILD_DIR}/bin
4. Make sure to run the app in root dir so shader files are found
5. Hit space in the app to turn ambient occulsion on and off
6. left textures should intermediate steps, check them in the end of the onDraw function
